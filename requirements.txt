# Core classical-ml-lab dependencies (Python 3.10–3.13)
numpy>=2.0
pandas>=2.2
matplotlib>=3.8
seaborn>=0.13
scikit-learn>=1.5
jupyter>=1.0
ipython>=8.0
pytest>=8.0
gymnasium>=0.28
attrs>=21.0
mdptoolbox-hiive>=4.0
setuptools>=65

# Randomized optimization (mlrose-hiive) needs joblib>=1.2,<1.3 (not a 3.13 ABI issue).
# For FlipFlop / Knapsack / TSP / NN-RO scripts:
#   pip install -r requirements-ro.txt
# classicallab lives under src/; smoke sets PYTHONPATH
