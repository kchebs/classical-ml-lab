# Core classical-ml-lab dependencies (Python 3.10–3.13)
numpy>=1.20
pandas>=1.3
matplotlib>=3.4
seaborn>=0.11
scikit-learn>=1.0
jupyter>=1.0
ipython>=7.0
pytest>=7.0
gymnasium>=0.28
attrs>=21.0
mdptoolbox-hiive>=4.0
setuptools>=65

# Randomized optimization (mlrose-hiive) is fragile on Python 3.13+.
# For FlipFlop / Knapsack / TSP / NN-RO scripts, use Python 3.10–3.12 and:
#   pip install -r requirements-ro.txt
# classicallab lives under src/; smoke sets PYTHONPATH
