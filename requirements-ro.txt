# Optional: randomized optimization runners (Python 3.10–3.13 OK).
# joblib MUST stay <1.3: mlrose-hiive imports joblib.my_exceptions (removed in 1.3+).
# Installing this will downgrade joblib relative to a default sklearn stack.
mlrose-hiive>=1.2.0
joblib>=1.2,<1.3
